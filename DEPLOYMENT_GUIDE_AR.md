# 🚀 دليل النشر السريع - Deployment Guide

## ✅ الإصلاحات التي تمت
1. **✅ إصلاح قائمة الجوال** - إضافة جميع الصفحات (التقارير، الحسابات، الإعدادات)
2. **✅ إضافة نافذة الميزانيات** - Budget Modal كانت مفقودة بالكامل

---

## 📋 خطوات النشر (Deployment Steps)

### الطريقة 1: استخدام clasp (موصى بها)

#### 1. تسجيل الدخول إلى Google
```bash
npx clasp login
```
سيفتح متصفح لتسجيل الدخول إلى حساب Google الخاص بك.

#### 2. ربط المشروع

**إذا كان لديك مشروع موجود:**
```bash
npx clasp clone <SCRIPT_ID>
```
استبدل `<SCRIPT_ID>` بمعرف المشروع من Google Apps Script.

**إذا كنت تريد إنشاء مشروع جديد:**
```bash
npx clasp create --type standalone --title "SJA Money Tracker"
```

#### 3. رفع الملفات
```bash
npx clasp push
```

#### 4. نشر الإصدار
```bash
npx clasp deploy
```

#### 5. فتح المشروع
```bash
npx clasp open
```

---

### الطريقة 2: النسخ اليدوي

#### 1. افتح Google Apps Script
اذهب إلى: https://script.google.com

#### 2. افتح مشروعك أو أنشئ واحد جديد

#### 3. انسخ الملفات المعدلة
فقط ملف واحد تم تعديله:
- **index.html** - افتحه من المشروع وانسخ المحتوى الجديد

#### 4. احفظ واختبر
- اضغط على "Deploy" → "Test deployments"
- افتح رابط التطبيق واختبر:
  - ✅ قائمة الجوال تظهر جميع الصفحات
  - ✅ زر "إضافة ميزانية" يفتح نافذة منبثقة
  - ✅ زر "إضافة عملية" يعمل

---

## 🔍 الاختبارات المطلوبة

### اختبارات أساسية
- [ ] قائمة الجوال: تظهر 6 صفحات
- [ ] زر "إضافة ميزانية": يفتح نافذة
- [ ] إنشاء ميزانية جديدة: يعمل
- [ ] تعديل ميزانية موجودة: يعمل
- [ ] حذف ميزانية: يعمل
- [ ] زر "إضافة عملية": يعمل (تأكد من عدم حدوث regression)

### اختبارات إضافية
- [ ] التنقل بين جميع الصفحات
- [ ] التحديث (Refresh) يعمل
- [ ] الإشعارات (Toasts) تظهر بشكل صحيح
- [ ] التحقق من الأخطاء (Validation) يعمل

---

## 📊 معلومات تقنية

### الملفات المعدلة
1. **index.html**
   - السطور ~805-820: قائمة الجوال
   - إضافة: Budget Modal قبل Account Modal

### النصوص البرمجية (Backend APIs)
جميع APIs موجودة في `WebUI.js`:
- ✅ `SOV1_UI_saveBudget()`
- ✅ `SOV1_UI_updateBudget()`
- ✅ `SOV1_UI_deleteBudget()`
- ✅ `SOV1_UI_getAllDashboardData()`

### المتغيرات (State Variables)
- ✅ `showBudgetModal`
- ✅ `editingBudget`
- ✅ `newBudget`
- ✅ `budgets`

---

## ❓ استكشاف الأخطاء (Troubleshooting)

### المشكلة: clasp غير معرف
```bash
npm install
```

### المشكلة: لا يمكن تسجيل الدخول
1. تأكد من تمكين "Google Apps Script API"
2. اذهب إلى: https://script.google.com/home/usersettings
3. فعّل "Google Apps Script API"

### المشكلة: الأزرار لا تعمل بعد النشر
1. افتح Developer Console في المتصفح (F12)
2. تحقق من وجود أخطاء JavaScript
3. تأكد من نسخ جميع التعديلات بشكل صحيح

### المشكلة: "انتهت مهلة التحميل"
1. تحقق من اتصال Google Sheets
2. راجع Script Properties في الإعدادات
3. تأكد من صلاحيات التطبيق

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. افحص سجل الأخطاء (Executions log) في Google Apps Script
2. راجع ملف `FIXES_JANUARY_2026.md` للتفاصيل
3. تأكد من تطبيق جميع التغييرات

---

## ✅ قائمة التحقق النهائية

قبل النشر:
- [x] تم تثبيت dependencies (npm install)
- [ ] تم تسجيل الدخول إلى clasp
- [ ] تم ربط المشروع (.clasp.json موجود)
- [ ] تم اختبار التغييرات محلياً

بعد النشر:
- [ ] اختبار جميع الصفحات
- [ ] اختبار جميع الأزرار والنوافذ المنبثقة
- [ ] التحقق من عدم وجود أخطاء في Console
- [ ] مراجعة سجل التنفيذ (Executions)

---

**الحالة:** ✅ جاهز للنشر  
**آخر تحديث:** 24 يناير 2026
